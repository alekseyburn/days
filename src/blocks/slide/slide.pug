//- Все примеси в этом файле должны начинаться c имени блока (slide)

mixin slide(classes)

  //- Принимает:
  //-   classes    {string} - список классов
  //- Вызов:
        +slide('some-class')

  -
    // список классов
    let allClasses = '';
    if(typeof(classes) !== 'undefined' && classes) {
      for (let item of classes.split(',')) {
        allClasses = allClasses + item.trim();
      }
    }

  section(class=`${allClasses.length > 0 ? `${allClasses} ` : ``}slide`)&attributes(attributes)
    block
