//- Все примеси в этом файле должны начинаться c имени блока (fashion)

mixin fashion(classes)

  //- Принимает:
  //-   classes    {string} - список классов
  //- Вызов:
        +fashion('some-class')

  -
    // список классов
    let allClasses = '';
    if(typeof(classes) !== 'undefined' && classes) {
      for (let item of classes.split(',')) {
        allClasses = allClasses + item.trim();
      }
    }

  section(class=`${allClasses.length > 0 ? `${allClasses} ` : ``}fashion`)&attributes(attributes)
    block
