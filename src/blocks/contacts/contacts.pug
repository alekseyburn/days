//- Все примеси в этом файле должны начинаться c имени блока (contacts)

mixin contacts(classes)

  //- Принимает:
  //-   classes    {string} - список классов
  //- Вызов:
        +contacts('some-class')

  -
    // список классов
    let allClasses = '';
    if(typeof(classes) !== 'undefined' && classes) {
      for (let item of classes.split(',')) {
        allClasses = allClasses + item.trim();
      }
    }

  section(class=`${allClasses.length > 0 ? `${allClasses} ` : ``}contacts`)&attributes(attributes)
    block
